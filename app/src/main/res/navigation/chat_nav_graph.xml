<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/chat_nav_graph"
    app:startDestination="@id/chatFragment">

    <fragment
        android:id="@+id/chatFragment"
        android:name="com.anviam.fragmentapp.ChatFragment"
        android:label="Chat"
        tools:layout="@layout/fragment_chat" />

    <fragment
        android:id="@+id/addUserFragment"
        android:name="com.anviam.fragmentapp.AddUserFragment"
        android:label="Add User"
        tools:layout="@layout/fragment_add_user" />

    <fragment
        android:id="@+id/historyFragment"
        android:name="com.anviam.fragmentapp.DateAndTimePickerFragment"
        android:label="History"
        tools:layout="@layout/fragment_date_and_time_picker" />
</navigation>
